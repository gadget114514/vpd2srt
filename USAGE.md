# VPD to SRT - 使用ガイド

## 概要

このアプリケーションは、VPDファイル（ビデオ編集プロジェクトファイル）から自動的にSRT形式の字幕ファイルを生成します。

## 機能一覧

### 1. VPDファイルの読み込み
- 📁 **VPDファイルを開く** ボタンをクリック
- ファイルダイアログから `.vpd` ファイルを選択
- 複数の字幕トラックに対応

### 2. 字幕のプレビューと検索
- 読み込まれた全字幕をリスト表示
- 🔍 検索ボックスで字幕を検索・フィルタリング
- タイムスタンプと字幕テキストを確認

### 3. SRTファイルの生成
- ✨ **SRTを生成** ボタンで字幕をSRT形式に変換
- 自動的にタイムスタンプを計算
- SRTプレビュー画面に出力

### 4. 保存設定のカスタマイズ

#### 出力ファイル名の指定
- テキスト入力フィールドで任意のファイル名を設定
- デフォルト：入力ファイルと同名の `.srt` ファイル
- `.srt` 拡張子は自動追加

#### 出力ディレクトリの選択
- 📂 **選択** ボタンをクリック
- ディレクトリダイアログから出力先を選択
- デフォルト：入力ファイルと同じディレクトリ

### 5. 出力オプション

**📋 コピー**
- SRTのテキスト全体をクリップボードにコピー
- 別のアプリケーションへ貼り付け可能

**💾 保存**
- 指定したファイル名とディレクトリにSRTファイルを保存
- UTF-8エンコーディングで保存

## 使用例

### 基本的な流れ

1. アプリケーションを起動
   ```bash
   npm start
   ```

2. **📁 VPDファイルを開く** をクリック
   - `202608-22-1_karaoke.vpd` などを選択

3. 字幕がリストに表示されることを確認
   - 検索ボックスで内容を確認

4. **✨ SRTを生成** をクリック
   - SRTプレビューが表示される

5. 出力設定を確認
   - ファイル名：`202608-22-1_karaoke.srt`
   - ディレクトリ：元ファイルと同じ場所

6. **💾 保存** をクリック
   - SRTファイルが保存される

## SRT形式について

生成されるSRTファイルの形式：

```
1
00:00:00,600 --> 00:00:03,400
即落ちの恋

2
00:00:03,399 --> 00:00:07,066
運命の出会いが欲しい

3
00:00:07,066 --> 00:00:12,500
この仲間うちは
どれだけ信心深いんだろ
```

### 形式の特徴

- **シーケンス番号**：順序を示す番号（1, 2, 3...）
- **タイムコード**：表示開始時刻 → 終了時刻
  - 形式：`HH:MM:SS,mmm` （時:分:秒,ミリ秒）
- **字幕テキスト**：複数行対応
  - `\N` で改行表示（VPDから自動変換）

## トラブルシューティング

### VPDファイルが読み込めない
- ファイルが JSON 形式のVPDであることを確認
- ファイルが破損していないかチェック
- ファイルのエンコーディングが UTF-8 であることを確認

### 字幕が検出されない
- VPDファイルに字幕トラック（SubtitleTrack）が存在するか確認
- TextEffectBlock に文字列データが含まれているか確認

### タイムスタンプが不正
- 元のVPDファイルのタイムスタンプを確認
- ミリ秒単位の計算が正確か確認

### 保存時のエラー
- 出力ディレクトリが存在し、書き込み権限があるか確認
- ファイル名に無効な文字が含まれていないか確認

## 技術仕様

### サポートされているファイル形式

| 形式 | 拡張子 | 説明 |
|------|--------|------|
| VPD  | .vpd   | JSON形式のビデオプロジェクトファイル |
| SRT  | .srt   | SubRip字幕ファイル（UTF-8） |

### 字幕トラック対応

- **複数トラック対応**：複数の字幕トラックがある場合は全て処理
- **オーバーレイ対応**：OverlayTrack内のテキストも処理可能
- **タイムスタンプ自動ソート**：入力順序に関わらず時系列で出力

### パフォーマンス

| パラメータ | 値 |
|-----------|-----|
| 処理速度 | < 1秒（一般的なファイル） |
| メモリ使用 | < 100MB |
| 最大字幕数 | 無制限 |

## キーボードショートカット

| ショートカット | 機能 |
|---------------|------|
| Ctrl+O (Cmd+O) | VPDファイルを開く |
| Ctrl+Q (Cmd+Q) | アプリケーション終了 |

## トライブ・テクニック

### ファイル名に日付を追加
```
202608-22-1_karaoke_2026-09-17.srt
```

### バッチ処理
複数のVPDファイルを変換する場合は、各ファイルについて手順を繰り返してください。

## サポートされるキャラクター

- 日本語（全角・半角）
- 英数字
- 特殊記号（`\N` は改行として処理）

## ライセンスと著作権

このツールは MIT ライセンスの下で公開されています。

## 更新履歴

### v1.0.0
- 初版リリース
- VPDからSRT生成機能
- GUI実装
- ファイルダイアログ対応
- 検索機能実装
